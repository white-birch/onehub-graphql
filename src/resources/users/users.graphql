enum AffiliateRole {
  ADMIN
  EMPLOYEE
  MEMBER
}

enum PortalRole {
  ADMIN
}

input CreateUserInput {
  email: String!
  password: String!
}

type User {
  id: ID!
  email: String!
  affiliateUserRoles: [AffiliateRole]!
  portalUserRoles: [PortalRole]!
}
