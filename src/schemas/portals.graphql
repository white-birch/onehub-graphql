type Portal {
  id: ID!
  affiliate: Affiliate
}

input CreatePortalInput {
  affiliate: AffiliateInput
}

type Query {
  portals: [Portal]
}

type Mutation {
  createPortal(input: CreatePortalInput!): Portal!
}
